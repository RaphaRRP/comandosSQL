DISTINCT = pegue resultados diferentes
count() = quantos tem?
where = onde procurar?
top = pega essa quantidade!
ORDER BY = ordernar em ordem crescente(asc) ou decrescente(desc)
BETWEEN e not BETWEEN = X esta entra MIN e MÁX(USAR DATA)
IN = Passar 2 ou mais valores. IN (x, y, z)
LIKE = pegar valores com tais caracteres. Like ('X' % 'Y').. % = Qualquer valor.
